{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline capability for the main window. Permissions are added per issue, never in bulk.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
