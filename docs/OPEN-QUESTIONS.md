# Open Questions

Decisions made with incomplete information. Each has a **current assumption** so work is not
blocked. Confirm or overrule them; changes go through an issue.

| # | Question | Current assumption |
| --- | --- | --- |
| 1 | "Grandstand at the end" — what exactly? | Interpreted as **Siegerehrung**: an animated podium showing 1st, 2nd and 3rd place, triggered manually by the host. If a standings *table* of all participants was meant, that is a separate scene and needs its own issue. |
| 2 | Are matches best-of-one? | Yes. The host clicks a winner; no scores, no sets, no draws. Adding scores later would touch the whole `Match` model. |
| 3 | Can a match end in a draw? | No. A winner must always be picked. |
| 4 | Final phase when fewer than 16 groups remain | The final phase starts at whatever power of two is reached (16, 8, 4 or 2) and names are entered then. A 32-group tournament reaches 16; a 10-group tournament reaches 8. |
| 5 | Is the repechage possible more than once? | No. Only after the qualifying round, because from then on the field is a power of two and halves cleanly. |
| 6 | Who decides accept/decline in the repechage? | The team decides, the host records it. Declining means elimination — the group does not return to the pool (except via the §4 fallback). |
| 7 | Team size / number of players per group | Not modelled. A group is one opaque unit with a number and, later, a name. |
| 8 | Are participant names needed earlier than the final phase? | No, but `settings.namingAt` makes the threshold configurable in case a host wants names from the start. |
| 9 | Sound effects | Out of scope for v1. If added: bundled locally, off by default, host-toggleable. |
| 10 | Multiple tournaments open at once | No. One tournament per window set. Switching files closes the current tournament (with a save prompt). |
| 11 | Repository visibility and licence | Repo created **private**. Licence undecided — add one before making it public. |
| 12 | Code signing the installer | Not planned. Windows SmartScreen will warn on first run; the host manual explains the click-through. |
| 13 | Printing / exporting results | Out of scope for v1. A PDF export of the bracket would be a natural v1.1 feature. |
| 14 | Touchscreen use by the host | Assumed mouse/trackpad. Hit targets are sized generously anyway (≥ 32 px). |
| 15 | The border tokens miss the 3:1 non-text contrast target | `--wm-border` reaches 1.48:1 and `--wm-border-strong` 2.00:1 against `--wm-bg`, against the 3:1 in STYLEGUIDE §1. The palette is implemented **as written** (issue #3) rather than quietly lightened, because the same greys are what keep a dark beamer from looking banded. Assumption: borders are decorative separators and never the only signal — a control that needs a visible edge uses `--wm-accent` or a status colour, both of which clear 3:1. Raising the border tokens is a palette change and needs its own issue. |
| 16 | How much chrome may the single-monitor preview beamer have? | The projected beamer is undecorated, cursorless and unselectable. The **preview** keeps its title bar and its cursor, and carries a `Vorschau` badge. Reasoning: with one screen the preview shares the monitor with the host UI, so an undecorated window the host cannot move or close, that also swallows their only pointer, is a trap rather than a faithful preview. Assumption: nobody in an audience is looking at a preview window, so fidelity loses to usability here. If a venue ever mirrors the laptop screen to the projector, this needs revisiting. |
| 17 | What counts as "a tournament is running" for sleep inhibition? | Currently **the beamer window being open**, because there is no tournament state until issue #5. Assumption: the condition moves to the real one once the store exists — an open beamer over-inhibits at worst, which is the harmless direction. |
| 18 | Which bundle identifier does the app ship under? | `at.deltadeveloping.wattmatt`, changed from the placeholder `at.wattmatt.app` in issue #4. It is not cosmetic: it decides `%APPDATA%\<identifier>\`, which issue #10 autosaves into, and the NSIS upgrade code that issue #31 ships. Assumption: the vendor namespace is correct and settled now, while no tournament file exists anywhere to be stranded — changing it after release would orphan every host's autosave and recovery data. |
| 19 | What does a snapshot carry before the domain model exists? | Issue #5 builds the host → beamer channel, but issue #7 owns the tournament model. The snapshot envelope is therefore final — revision, scene, `autoFollow`, delivery — while its `tournament` field carries only `groups` (id, number, optional name), the one entity already fixed by docs/GLOSSARY.md. Assumption: #7 extends `tournamentSnapshotSchema` in place and leaves the envelope alone. The channel is profiled against 64 groups, but against the placeholder group shape — roughly 3 KB, not the few hundred KB ARCHITECTURE §3 anticipates. The 16 ms number is therefore a floor, not a proof, and is re-measured when #7 lands the real snapshot. **Settled in #7:** the placeholder group shape is gone — the snapshot now carries the real `Group`, so there is one definition rather than two. It still carries *only* groups: the scenes that need rounds, tables and the bracket are #18, #19, #25 and #27, and each extends the schema with what it draws rather than #7 guessing at it. The 16 ms budget still holds at 64 groups, but remains a floor until a scene sends more than groups. **Extended in #13:** the snapshot now also carries `tables` and the matches that are on them — the real `Table` and `Match`, not a view of them — because `TABLE_OVERVIEW` draws exactly those. `matches` deliberately holds only the matches currently on a table; #19 widens the same field to a whole round rather than adding a second one. The budget was re-measured against the heavier payload: 64 groups on 32 busy tables still round-trips inside one frame (`snapshotPerformance.test.ts`). |
| 20 | Issue #5 adds mutating actions before the undo stack exists | `showScene`, `blackout` and `setAutoFollow` (issue #5) commit without landing on an undo stack, because the stack is issue #11. The Definition of Done box for undo therefore fails on that branch by construction rather than by oversight. Assumption: #11 hooks into `TournamentStore.commit`, which is the single funnel every action already goes through, and no action written before then needs changing. If #11 finds it needs per-action metadata instead, these three actions are the first to revisit. **Extended in #9:** `setNewDocument`, `setOpenedDocument`, `setDocumentSaved` and `closeDocument` commit the same way and are on the same footing — the undo box on that branch fails by construction too. One of them is different in kind, though, and that part is *not* deferred: **closing a tournament is not undoable even once #11 lands.** The stack #11 builds is in memory and does not reach across a document switch, so the state undo would restore is the state that was just discarded. The unsaved-changes dialog is therefore the only thing standing between a misclick and a lost tournament, which is why *Verwerfen* is its quietest button and why the flow is tested end to end in `useTournamentDocument.test.tsx` rather than left to inspection. Assumption: #11 does not attempt to make document switches undoable. If it ever should, the tournament that was closed has to be kept in memory, and that is a change to the store's shape rather than to these actions. **Settled in #11:** the stack hangs off `TournamentStore.commit` exactly as assumed, and none of the seven actions needed changing — the three beamer actions gained a German label and the four document actions were left without one on purpose. That is what clears the stack: a commit that rewrites the tournament without saying what the host did is the document being replaced, not a decision inside one, so the clearing is structural rather than a rule each document action has to remember. Document switches remain outside undo. |
| 21 | FILE-FORMAT.md stored German enum values (`ACHTELFINALE`, `GRUPPE`) | Changed to English (`ROUND_OF_16`, `GROUP`) in issue #7, and the document updated to match. CLAUDE.md golden rule 1 is unambiguous that code is English, and a stored enum value is code — it is compared, switched on and indexed by, never displayed. The German names are UI copy and live in `de-AT.ts` where the glossary says they belong. Assumption: no `.wattmatt` file exists anywhere yet, so this costs nothing now and would cost a migration later. The lint rule cannot catch this class of slip, because `ACHTELFINALE` contains no umlaut — it is caught by review only. |
| 22 | What happens to a group's number when the host removes it during SETUP? | Not decided by issue #7. `GroupStatus` is `ACTIVE \| ELIMINATED` — the tournament lifecycle — and removal is left to issue #14 (group management). TOURNAMENT-RULES §2 requires that numbers are never reused, which a plain delete plus `max(number) + 1` would violate the moment the highest-numbered group is removed and another added. Assumption: #14 picks either a `WITHDRAWN` status or an explicit next-number counter, and that choice is a schema change it owns. Recorded here so it is decided rather than discovered mid-event. **Settled in #14: the explicit counter.** `tournament.nextGroupNumber` is stored in the file and only ever goes up, so a removed group's number is spent for good — the same mechanism, and the same reasoning, as `nextTableNumber` in #37. A `WITHDRAWN` status was the alternative and was rejected: it would leave a participant on every screen that lists the field, and the host's actual problem is a row that was entered twice and has to *disappear*. It cost a `SCHEMA_VERSION` bump to v3 and the migration `v2_to_v3`, which reconstructs the counter from the highest `grp_<n>` a v2 file still mentions anywhere. What removal does **not** do is renumber: deleting 3 of 5 leaves 1, 2, 4, 5 (§9 case 9). Assumption: nothing derives a group's position in the field from its number — the numbers are a sequence with holes in it from the first deletion on. |
| 23 | Issue #8 delivers the RNG but two of its bullets land elsewhere | #8 lists "seed generated from `crypto.getRandomValues` at tournament creation" and "persist `rngSeed` and `rngCursor` so a reloaded tournament continues the same stream". Neither is buildable at #8's layer: nothing creates a `Tournament` yet (the store holds a `TournamentSnapshot`, and "Neues Turnier" is issue #9), and nothing consumes randomness yet, so there is no cursor to write back until the draw engine in #16. #8 therefore ships the **mechanism** — `createRng(seed, cursor)`, `generateSeed()`, and the schema fields that already exist — and is closed on that basis rather than left open with nothing actionable in it. Assumption: **#9 calls `generateSeed()` once when creating a tournament and never again, and #16 writes `rng.cursor` back into `rngCursor` on every committed draw.** Until #16 does, acceptance criterion "replaying (seed, cursor) reproduces a full tournament draw-for-draw" is demonstrated only at the RNG level — `createRng(seed, cursorAfterFirstDraw)` reproduces the next draw exactly — not end to end. If either issue lands without its half, a reopened tournament silently re-draws pairings the room has already watched, which is the failure this whole mechanism exists to prevent. **Half settled in #9:** `generateSeed()` is called exactly once, in `createTournamentDocument`, and the seed is written to the file and never touched again — `persistence.test.ts` asserts both the single call and `rngCursor: 0`. **Settled in #16:** `drawRound` (`@/domain/draw`) writes `rng.cursor` back into `rngCursor` in the same object that carries the pairings it produced, and defaults its generator to `createRng(rngSeed, rngCursor)` so a caller cannot draw from the wrong position by accident. `draw.test.ts` pins both halves: a draw replayed from the stored `(seed, cursor)` reproduces the round exactly, and a redraw from the advanced cursor does not. |
| 24 | Which folder is the tournament library — `%APPDATA%\WattMatt` or `%APPDATA%\<bundle identifier>`? | **`%APPDATA%\WattMatt\tournaments`**, as docs/FILE-FORMAT.md §"Location" writes it, implemented in issue #9. This is *not* Tauri's `app_data_dir()`, which follows the identifier settled in #18 and would give `%APPDATA%\at.deltadeveloping.wattmatt`. Reasoning: the file format promises a tournament can be repaired in Notepad and copied onto a USB stick, and both start with a host finding the folder in Explorer — which they do by product name, not by reverse-DNS. Assumption: issues #10 (autosave, backups) and #30 (`logs/`) use the same `%APPDATA%\WattMatt` root, so there is one WattMatt folder rather than two. If the installer of #31 ever needs the identifier-based path for uninstall cleanup, that is a #31 concern and does not move the library. |
| 25 | What are the "recent tournaments" on the start screen? | **The default library, listed from disk, newest first** — not a remembered most-recently-used list. Implemented in #9. Reasoning: a remembered list is a second source of truth that goes stale in both directions. A tournament copied onto the laptop from a USB stick would not appear until it had been opened once, and one deleted in Explorer would keep being offered until the host clicked it and got an error. Assumption: a host who wants a tournament from outside the library uses *Turnier öffnen*, which is one click away. If the library ever grows past what a screen can show, the list needs a cap and a sort, not a remembered MRU. |
| 26 | When does a new tournament first reach the disk? | **Immediately on creation**, into the library under a file name derived from its name and made unique against what is already there (#9). Reasoning: the alternative — in memory until the first explicit *Speichern* — leaves a window in which a flat battery costs the host everything they have entered, and it leaves the autosave of #10 with no target. Assumption: the host would rather have a file they did not ask for than lose one they did. A write that fails is not fatal: the tournament stays open, the state line reads *Noch nicht auf der Festplatte*, and *Speichern unter…* is the way out. |
| 27 | Issue #9 lists "preserve unknown fields on save" but ships a strict schema | **The task moves to #12** (migration framework), and `tournamentFileSchema` stays strict in #9. Reasoning: forward compatibility only does anything once a *second* schema version exists — it protects a file written by a newer build from being stripped by an older one, and there is exactly one version, no released build, and no `.wattmatt` file anywhere. Against that, a permissive schema costs something real now: `schema.test.ts` detects a field forgotten in `tournamentSchema` precisely because an unknown key does *not* survive the round trip, and loosening the schema would disarm that check for every issue between here and #12. Assumption: #12 lands the passthrough together with the migration it exists to serve, and owns replacing the forgotten-field check with one that does not depend on strictness. Recorded here, and on issue #9, rather than settled by editing FILE-FORMAT.md — re-scoping an issue is the issue author's call, not the branch's. | **Settled in #12:** the passthrough landed with the migration framework, and at the top level only. `carriedFields` picks up the unknown top-level keys of an opened file, the store holds them beside the tournament, and `withCarriedFields` writes them back on every save — nested objects still parse strictly, so the round-trip assertion in `schema.test.ts` keeps catching a field forgotten under `settings` or in a `match`. The top-level half of that guard was replaced rather than dropped: `covers every top-level field of the documented example` compares the schema's keys against the document's in both directions, which does not depend on strictness at all. |
| 28 | What does *Änderungen verwerfen* still mean once autosave is running? | **It survives only for a tournament that has never reached disk.** Closing a tournament or the window now flushes the pending autosave first (#10), so a tournament with a file is written before the question would have been asked — and the question is not asked. Reasoning: with a 500 ms debounce, "discard" could only ever discard the last half-second, and offering a host a button that undoes an arbitrary fraction of what they just did is worse than not offering one. The dialog stays for the `unsaved` file state, where closing genuinely loses everything and the app cannot decide on the host's behalf. This narrows the guarantee recorded in #20, which was written before autosave existed. Assumption: undoing a decision is issue #11's job, not the close dialog's. If a host ever needs "close without saving what I just did", that is a request for undo, and it should be answered there. |
| 29 | Rotating three backups before *every* save makes them minutes apart, not hours | Implemented as docs/FILE-FORMAT.md rule 3 and issue #10 specify: every save rotates, and the acceptance criterion ("backups rotate correctly over 50 consecutive saves") is written on that basis. The consequence is worth stating plainly, because it is not what "three backups" sounds like: during an active round the chain covers roughly the last three autosaves, which can be under a minute of an event. Reasoning: the failure the backups actually defend against is a corrupt or truncated *current* file, and for that the previous three writes are exactly the right thing to hold. Recovering the tournament as it stood an hour ago is not a case anybody has asked for, and would be a different feature — a timed snapshot chain — not a deeper rotation. Assumption: if a host ever does need to go back further, the answer is periodic snapshots under their own names, decided in their own issue. |
| 30 | An autosave failure cannot be dismissed | The `autosaveFailed` notice is the only one in the host UI with no *Ausblenden* button (#10). Reasoning: every other notice reports something that already happened and is over; this one reports a condition that is still true while the host reads it — the stick is still out, the file is still locked — and nothing is being written for as long as it is. A banner they can hide is a banner they will hide, and the next thing they would learn about it is the crash. It clears itself the moment a write succeeds, and it carries *Speichern unter…* so there is always something to click. Assumption: the host would rather lose a strip of screen than lose the tournament. |
| 31 | A save marks the file clean — but the host kept clicking while it was being written | **Only a save whose `documentRevision` still matches marks the file `saved`;** anything else leaves it `modified` and lets the scheduled autosave catch up (#10). Reasoning: the write is asynchronous and the debounce is 500 ms, so during an active round most writes overlap the host's next decision. Marking the file clean regardless would tell them a result is safe when it is not in the bytes on disk — and the crash a second later is exactly the scenario autosave exists for. A second counter was needed rather than reusing `revision`, which also moves for staging a beamer scene and for taking manual control: keying on it would mark a current file unsaved because the host pressed a beamer button mid-write, at the cost of a redundant write and a backup rotation each time. Assumption: no future action mutates `document` without going through `commit`, which is what makes `documentRevision` a complete record of what a file could contain. If one ever does, this guard silently stops working, and so does the dirty flag it sits next to. |
| 32 | Issue #11 asks undo to restore the RNG cursor **and** asks a redraw after an undo to differ from the draw undone | **The cursor is never rewound.** It is a stream position, not tournament state, and it is left out of the undo snapshot's type together with the append-only `log` (`UndoDocument` in `src/store/undo.ts`). Reasoning: the two lines of the issue cannot both hold — restoring the cursor makes a redraw reproduce the pairing the host has just rejected, which is the acceptance criterion, and the one the room would notice. Rewinding also breaks the property the seed exists for: two different draws would share one `(seed, cursor)`, and a disputed pairing could no longer be reproduced from the file (CLAUDE.md golden rule 7). What the task line asks for is still delivered — undo restores everything *derived*, the round, the table occupancy, the matches the draw created — the cursor is simply not derived from anything. Assumption: issue #16 writes the cursor back after each draw and never reads it as "how many draws are in the tournament". If a scene ever needs that number, it counts rounds, not RNG values. **Held in #16:** `drawRound` writes the cursor and never reads it as a count — the round number comes from `rounds.length`. `draw.test.ts` plays the undo case out: with the round removed and the cursor left where it was, the redraw pairs the field differently. |
| 33 | Are the beamer actions undoable, when the host mostly wants the last *tournament* decision back? | **Yes, they land on the stack like everything else** (`showScene`, `blackout`, `setAutoFollow`), and the history is strictly linear: `Ctrl+Z` walks back through a blackout before it reaches the winner set before it. Reasoning: golden rule 6 says every mutation lands on the stack, and a stack with exceptions is one the host has to keep a model of. The visible cost is real — an undo aimed at a result may take two presses — and it is paid for by the buttons naming the step they would take, so the host reads *what* is about to be taken back rather than guessing. **Refined after review:** landing on the stack does not mean costing what a decision costs. An undo of a beamer action leaves the tournament, the log and the file alone and goes out on the scene channel, exactly as the action itself did — an undo that rewrote the tournament would put the host's correction of the projector behind sixty-four groups of data and dirty a clean file for it (`UndoEntry.touchedDocument`, docs/FILE-FORMAT.md rule 6). Assumption: issue #28 builds the beamer control centre on the same actions. If staging scenes turns out to be frequent enough that it buries tournament decisions, the answer is a second, scene-only history rather than silently unrecorded actions. |
| 34 | A file has to be migrated and the copy of the original cannot be written | **The file is not opened.** The pre-migration copy (`name.wattmatt.v1.bak`, docs/FILE-FORMAT.md rule 7) is the one backup in WattMatt that is not best effort, and issue #12 implements it that way. Reasoning: rule 3's rotation failing costs a recovery point the host still has two more of, while this copy failing means a file is about to be reinterpreted with nothing holding what it said before — and the autosave rewrites it in the new format within half a second of the host's first click. A medium that refuses a copy next to a file it just read is failing or read-only, so "open it anyway" is a promise the next save is likely to break too. Assumption: the host would rather be told to move the file somewhere writable than lose the only copy of a tournament from the previous version. If this ever turns out to fire on a working machine, the answer is a copy into `%APPDATA%\WattMatt` as a fallback location, not opening without one. |
| 35 | What happens to a running match when the host deletes or disables the table it is on? | **The host is asked, and there is no "leave it there" answer** (issue #13). The table is going away, so the match has to go somewhere: back into the queue as `WAITING_FOR_TABLE`, or straight onto another free table, carrying its start time with it. A move whose target is not free is refused **as a whole** — the table stays, the match stays — rather than half-done, because a match that has fallen off the board is invisible, and invisible is how a match gets forgotten in front of an audience. Reasoning: the two answers are the two things that actually happen in a room when a drink goes over a table, and the app cannot know which. A free table is deleted with no question at all, because there is nothing to ask and undo is the way back from a misclick. Assumption: issue #16 offers a freed table to the queue rather than assigning it, so a requeued match reappears the moment the host confirms it. If "block this table once the current match finishes" turns out to be what hosts want, that is a pending state on the table and needs its own issue. |
| 36 | How does the occupancy board know how long a match has been running? | **`occupiedSince` on the table, in the file** (issue #13, schema v2). Reasoning: the alternative — a timestamp held in memory when the match is assigned — reads correctly right up to the moment it matters, because a laptop restarted mid-event would report every match as just begun, and "works with a tournament loaded mid-tournament" is a Definition-of-Done box. Putting it in the file cost a `SCHEMA_VERSION` bump and the first real migration, which is what that framework exists for. The stamp travels with the *match*: moving a running match to another table carries it across, because the room has been watching that match for twenty minutes whichever table it sits on. Assumption: nobody needs the time a table has been *free*. If a host ever asks which table has been idle longest, that is a second stamp, not a reinterpretation of this one. |
| 37 | A table is deleted mid-tournament — what happens to the matches already played on it? | **They keep pointing at it.** `match.tableId` is a record of where a match was played, and rounds already closed are not rewritten (issue #13). Reasoning: the alternative is either rewriting history, or refusing to delete a table that has ever been used — which would mean a broken table stays on the board for the rest of the event. The reference cannot go stale in a harmful way, because a table number is never reused: `tournament.nextTableNumber` is a stored counter that only ever goes up, so a deleted `tbl_2` never comes back on a different piece of furniture. **It has to be stored, not derived:** once `tbl_3` is gone, nothing left in `tables` remembers that 3 is spent, so `max(tables) + 1` would hand it straight back — which is exactly what this answer leans on not happening. A v1 file has no counter, so the migration reconstructs it from the highest `tbl_<n>` the file still mentions anywhere, tables and matches and bracket nodes alike (`@/domain/migrations/v1_to_v2`). Assumption: no scene resolves `match.tableId` without tolerating a miss. The occupancy board already does — a table whose match cannot be found is drawn and named rather than skipped. |
| 38 | Issue #13's first acceptance criterion says marking a winner frees the table — but nothing marks a winner yet | **The transition exists and is tested; the caller is issue #17's.** `releaseTable` (`@/domain/tables`) is the one place a table goes back to `FREE`, and `disableTable`, `removeTable` and the move-target dialog already go through it. What #13 does not ship is a "Sieger" button to reach it from, because the round panel that owns one is #17 and the draw that fills the tables is #16. Reasoning: writing a winner action here would mean guessing at #17's model of a round and having two of them a week later. The criterion is therefore **carried, not met**, and #17 closes it by calling `releaseTable` rather than by writing a table by hand. Assumption: #17 is implemented before an event. Until it is, a table is freed by the host through the table panel — deleting or disabling and re-enabling it — which is a workaround, not the feature. |
| 39 | Two tables with the same label | **Refused on rename, tolerated on create** (issue #13). `renameTable` will not give a table a name another table already answers to, compared case-insensitively: the label is what the host says out loud and what the move-target dropdown offers when a busy table is deleted, and two identical options there is a match sent to the wrong table in front of the room. The refusal is silent — the field puts the old name back, the same way it does for an emptied one — because a dialog for a name that can simply be retyped is worse than the correction. Reasoning: the counter in #37 already keeps generated labels ("Tisch 4") unique, so the only way to collide is by typing it. The one gap left is a host who renames a table to a *future* default name and then adds tables past it; `addTables` does not check, because the alternative is the domain inventing a German fallback label, which golden rule 1 forbids. Assumption: that is rare enough to leave to the host, who can see both rows. If it turns out not to be, the answer is a uniqueness suffix chosen in `de-AT.ts` and passed in like every other label. |
| 40 | May a group be removed once the tournament is under way? | **No — a group that has been drawn is refused** (issue #14, `isRemovable` in `@/domain/groups`). A match, a repechage draw or a bracket slot *names* the group, and deleting it would put a pairing against nobody on the beamer — a `Freilos` the draw never granted (docs/TOURNAMENT-RULES.md §0). Reasoning: the alternative is either rewriting rounds that have already been played, or drawing a match card with an empty side in front of the room. Neither is something a host can explain to a participant. The refusal is visible rather than silent: the chip's delete control is greyed out and says why, because a control that disappears leaves the host looking for it. A participant still leaves the tournament in the three ways the rules already have — losing, declining a repechage slot (§4), or the host undoing back past the draw. **Adding** is the opposite case and is always allowed (see #41). Assumption: a participant who has to be struck from a running tournament is rare enough that undo plus a fresh draw is an acceptable answer. If it turns out not to be, the answer is a `WITHDRAWN` status that keeps the record and stops the pairing, not a delete. |
| 41 | May a group be added after the draw? | **Yes, with a warning** (issue #14). The host is in control (CLAUDE.md golden rule 3), and a participant turning up ten minutes late is the most ordinary thing at a club evening. What the app owes them is the consequence, said before the beamer says it: a group added now is not in the rounds already drawn and joins at the next draw. Reasoning: a refusal would send the host to a text editor, and a silent add would have them discover the missing pairing on the projector. The warning is keyed on `rounds.length > 0` rather than on `phase`, because a round is the thing a late entry would have had to be in. Assumption: issue #16 draws each round from the groups that are `ACTIVE` at that moment, so a late entry needs nothing beyond existing. |
| 42 | Does the beamer know whether the room is playing in `Gruppen`, `Teams` or as `Spieler`? | **Yes — `participantLabel` travels in the snapshot** (issue #14). It is the only part of `settings` the beamer is sent, because it is the only one that changes what the audience reads. Reasoning: the host screen and the projector calling the same participant two different things is precisely the disagreement golden rule 4 exists to prevent, and the alternative — the beamer assuming `Gruppe` — is wrong for two of the three settings. `groupLabel` therefore takes the wording as an argument with no default, so a screen that forgot to thread it through fails to compile rather than saying the wrong word all evening. Assumption: the wording never differs per scene. If a scene ever wants a shorter word for space reasons, that is a second entry in `de-AT.ts`, not a second setting. |
| 43 | Does the beamer need to know about `settings.performanceMode`? | **Yes — it travels in the snapshot** (issue #15). docs/MOTION.md §6 requires performance mode to be switchable *mid-event without reloading the beamer window*, and the snapshot is the only channel that reaches a window already showing something; a preference file read at start-up would take effect one restart after the host needed it, in front of the audience. It lands on `.beamer-root` as `data-performance-mode`, and `src/styles/global.css` redefines the duration and stagger tokens under that attribute — so a scene written for #18 or #25 is covered without its author remembering to check a flag. What #15 ships is therefore the **plumbing plus the durations**, which is all of MOTION.md's list that exists today: there are no particles, no blur and no glow in the app yet. Issue #29 shipped those: `--wm-fx-glow-spread` and `--wm-fx-blur` join the durations as tokens the attribute zeroes, particles branch on the flag in the scene that draws them, and the whole block still redefines nothing but custom properties — so the mode cannot move a box while the room is watching. Assumption: halving every duration is the right blunt instrument for a laggy projector, and #29 refines rather than replaces it. If a scene ever needs a *different* cheap version of itself rather than a faster one, that is the scene's own branch on this flag, not a second setting. |
| 44 | How much of a table shortage is worth warning about before the start? | **More than two sittings** — the warning fires when `matches > tables × 2` (`TABLE_SHORTAGE_FACTOR` in `src/domain/start.ts`, issue #15). Reasoning: eight participants on two tables plays four matches in two goes, and nobody in the room would call that a problem; at three sittings the last pair is waiting on two full rounds in front of them, which is worth knowing while carrying another table in is still an option. The warning names the estimated queue length rather than just existing, because "17 Partien warten" is a number a host can act on and "zu wenige Tische" is not. It is emphatically **not** a block: how long a queue an evening accepts is the host's decision (CLAUDE.md golden rule 3). Assumption: the factor is a judgement call and not a rule from docs/TOURNAMENT-RULES.md, so it lives in one constant with the reasoning next to it. If hosts find it either naggy or silent, the fix is that number. |
| 45 | What does *Turnier starten* actually do, when the draw engine is issue #16? | **It moves the phase from `SETUP` to `QUALIFYING`, and nothing else** (issue #15, docs/TOURNAMENT-RULES.md §1). Reasoning: the alternative — starting *and* drawing in one click — would put the first round's pairings on the projector the instant the host presses the button, which is the opposite of golden rule 3. A host starts the evening, says so out loud, and draws when the room is standing at the tables. It also keeps #15 shippable without guessing at #16's model of a round. The consequence to be aware of: between the two issues, a started tournament is a tournament in `QUALIFYING` with no rounds in it, which is a state every reader has to tolerate — `hasStarted` in `@/domain/groups` keys on `rounds.length` rather than on the phase for exactly this reason. Assumption: #16 draws the qualifying round as a separate, explicit host action and does not fold the start into it. |
| 46 | Which settings may be changed while the tournament is running? | **All of them except the naming threshold** (issue #15). The tournament name, the participant wording and performance mode are reachable in every phase: a host reconfigures during an event, and a panel that freezes itself the moment the tournament starts sends them to a text editor. `settings.namingAt` is locked from `NAMING` on, because by then the app has told the room that this is the field size at which names are entered, and moving the line afterwards either demands names nobody was asked for or leaves a bracket half-named. The lock is a greyed-out field with the reason beside it rather than a control that disappears — a host who cannot find a setting assumes the app lost it. Assumption: no later phase needs to freeze the participant wording. It renames nothing and moves nothing, so switching from `Gruppen` to `Teams` during the `Siegerehrung` is odd but harmless. |
| 47 | Issue #16 sketches `drawRound(groups, tables, rng): Round` — why does the engine take and return a whole `Tournament`? | **Because a `Round` on its own cannot say what the draw did** (issue #16). One draw occupies tables, stamps them with a start time, advances the RNG cursor and appends a round, and those four have to land in the same commit or the projector and the laptop disagree about who is playing where (CLAUDE.md golden rule 4). Every other mutation in `src/domain` already has the shape `Tournament -> Tournament` (`@/domain/tables`, `@/domain/groups`, `@/domain/start`), so the store action wrapping it is one line and the undo snapshot is the document, unchanged. What the issue's signature asked for is still available and still pure: `roundOutcome(round)` reads winners and losers out of a round without a tournament in sight, which is what #20 needs. Assumption: no caller needs to draw a round *without* committing it. A preview that consumed randomness would move the stream (docs/OPEN-QUESTIONS.md #23), so `previewFirstRound` in `@/domain/start` counts instead of drawing. |
| 48 | `matchStatus` has a `READY` value — when is a match `READY` rather than `RUNNING`? | **Never, for now: a match starts when it is put on a table** (issue #16). `occupyTable` (`@/domain/tables`, issue #13) moves a match straight to `RUNNING` and stamps `occupiedSince`, and the draw engine goes through that one transition rather than writing a table by hand, so `READY` is a value the file format allows and nothing produces. Reasoning: the alternative is a second host action — put it on the table, then start it — which is a click that buys nothing at a club evening, and `occupiedSince` would then mean two different things depending on which of the two had happened. Assumption: hosts do not want a table reserved for a pair that has not walked up yet. If they do, `READY` is where that state goes and the occupancy board learns to draw it; the value is deliberately left in the schema so adding it is not a migration. |
| 49 | What stops a second qualifying round being drawn once the first is closed? | **A blocker, `QUALIFYING_ALREADY_DRAWN`** (issue #16, docs/TOURNAMENT-RULES.md §3, which calls `QUALIFYING` round 1). While the phase is still `QUALIFYING` and a qualifying round exists, `canDrawRound` is false, so the draw button is disabled and a stale click costs nothing. Reasoning: without it, a second press would deal the qualifying round's winners into another qualifying round and the repechage would compute its target from the wrong field. Moving the phase on — to `REPECHAGE` or to `ELIMINATION` — is issue #22's, and until it lands the engine draws exactly one qualifying round and then stops, which is a state the round panel has to be able to show. Assumption: #22 sets `phase` before asking for the next draw, rather than adding a second entry point that bypasses the blockers. |
| 50 | How is a result that is already on the projector protected from a stray click, when setting one has to stay a single click? | **The card is armed first** (issue #17). An undecided match shows its two targets and one click decides it, with no confirmation — that is the first acceptance criterion, and it is affordable because every decision is undoable (golden rule 6). A **decided** match shows its result instead, and its targets come back only after the host presses *Ergebnis ändern*: two deliberate interactions, of which the first changes nothing. Reasoning: the alternatives are a confirmation dialog on every result, which taxes the ninety-nine correct clicks to catch the one wrong one, or a long-press, which is undiscoverable and hostile on a trackpad. The armed card is panel state and not card state, so arming a second card disarms the first — a screen with four cards left open is four chances for the click this exists to prevent. Assumption: correcting a result stays rare enough that the extra press is not felt. If it turns out not to be, the arming can widen to a whole section rather than becoming automatic. |
| 51 | Does *Auslosung starten* take the beamer even when the host has taken manual control of it? | **Yes, and it is the same commit as the draw** (issue #17). The scene and the round land together, so one undo takes back both — a projector left showing the pairings of a round that no longer exists is the failure golden rule 4 exists to prevent, and a picture taken back while the round stayed drawn would have burned the RNG cursor for nothing. `autoFollow` itself is left as it was: the draw button *is* an explicit host instruction, so a host in manual control gets the picture they just asked for without silently being put back into automatic (golden rule 3). Reasoning: the alternative — staging only while `autoFollow` is on — means the most theatrical moment of the evening quietly does not appear on the projector, and the host finds out from the room. Assumption: issue #18 animates into this same scene descriptor, and issue #22 owns what `autoFollow` implies per phase. |
| 52 | Is the live repechage target read off the winners decided so far? | **No — off the number of matches in the round** (issue #17, `repechageOutlook` in `@/domain/round`). Every match produces exactly one winner, byes included, so `\|W\|` at the close of a round is fixed the moment the round is drawn. Reasoning: a summary that counted decided winners would say *Ziel 8* at half time and *Ziel 16* at the end, and a host reading the second number would think something had gone wrong — while the number they actually want, the size of the field the bracket will need, is available from the first second and is what lets them plan the evening. The winners and losers *lists* beside it are still live, because those answer a different question: who is through so far. Assumption: issue #20 computes the real target from the same `nextPowerOfTwo`. |
| 53 | `Space` skips the draw, but only while the beamer window has focus | Issue #18 registers the skip in the beamer window itself. With two screens the host is normally typing on the laptop, so the key goes to the *host* window and nothing happens — the skip works reliably only by clicking the projector window first, which on a live stage is not a thing anyone will do. Routing a host keypress to the projector needs a message the event contract does not have: `beamer:scene` carries scenes, not commands, and inventing a `beamer:command` channel here would pre-empt the beamer control centre that owns exactly this (issue #28). Assumption: **#28 adds a host-side skip control and the channel under it**, and `useSkipKey` moves behind it. **Answered by issue #28:** the host's `Space` bumps `skipToken`, a monotonic counter carried in the picture itself, and `useSkipSignal` skips when the number the beamer is holding changes. No `beamer:command` channel was invented — a counter *is* truth, so it rides the ordered channel the rest of the picture does, a re-delivered snapshot skips nothing, and a beamer reopened after five skips does not fire five of them. `useSkipKey` stays alongside it for the single-screen venue where the host can click into the beamer window. Before that the sequence was bounded rather than unbounded — 32 pairings run for 90 s and end on their own — so the failure mode is a host waiting, not a host stuck. The animation never blocks state either way (golden rule 5): the round is already drawn and committed before the first beat plays, so the host can close the round, mark a winner or stage another scene while it is still running. |
| 57 | Fitting every table, team and match on the wall means going below the 32 px floor docs/STYLEGUIDE.md §2 calls absolute | **Showing everything wins, and the floor becomes a design target** (issue #55). Scenes take as many columns as the field needs and then scale the whole body down with `zoom` until it fits the stage; nothing is sliced, counted or clipped. Reasoning: the stage is `overflow-hidden`, so the old behaviour was not "the rest is too small to read", it was "the rest is not there" — and the pair whose match was the fourth in its section, or the participant whose chip was the sixty-fifth, is precisely the person who came to look at the wall. A row that ended up small can be read by walking closer or by asking; a row that was never drawn cannot be read at all, and nobody in the room can even tell it is missing. The scenes still put the ordinary field sizes on or above 32 px, so the floor keeps doing its job for the evenings it was written for. **The measurement is the load-bearing part:** `useFitToStage` measures the frame against the natural height of the content rather than estimating from a count, because how tall a board comes out depends on the wording in `de-AT.ts` and on how many lines a card ends up with, and an estimate that is optimistic by one row puts a match on the floor behind the projector. Assumption: a host who reaches an unreadable size wants to be shown everything and to fix it by adding a round or a screen, not to have the app choose for them. If that turns out to be wrong, the answer is a warning on the *host* window when `--wm-fit` goes below the floor — never a scene that silently drops rows.
| 58 | Why `zoom` for the shrink, rather than a smaller font size or `transform: scale()`? | **Because the boxes have to get smaller, not just the text** (issue #55, the `beamer-fit` utility). `zoom` multiplies every length inside the element — type, padding, borders, radii and grid gaps — so a card at 60 % is the same card, whereas a font-size change would leave the text rattling around inside padding that stayed where it was, and the board would run out of room again long before the type did. It also re-lays the content out at the new size, so the projector renders real glyphs; `transform: scale()` magnifies the pixels of a layout done at the original size, which is the difference between a small row and a blurry one at ten metres. The cost is that a card at 60 % also has 2.4 px of border rather than 4 (docs/STYLEGUIDE.md §5) — accepted, on the same reasoning as #57. Assumption: the beamer window is Chromium (WebView2 on Windows, Tauri 2), where `zoom` is fully supported and has been for as long as the property has existed.
| 54 | Issue #16 left the phase alone (docs/OPEN-QUESTIONS.md #49) — why does `startRepechage` set it? | **Because entering the repechage *is* the phase change** (issue #20, `@/domain/repechage`). `startRepechage` writes `phase: 'REPECHAGE'`, the target and the shuffled pot in one object. Reasoning: a draw and a phase are two different decisions — the host draws round 2 *while* the tournament is already in `ELIMINATION` — but there is no such thing as being in `REPECHAGE` without a pot, or holding a pot while the phase still says `QUALIFYING`. The two halves would be observable apart on the projector, and in a file recovered after a crash between them, which is the failure golden rule 4 exists to prevent. What #22 still owns is everything on either side: deciding to enter the phase at all (`isRepechageNeeded`, which is false for a field that is already a power of two and must skip §4 without showing an empty scene), and leaving it once `isRepechageComplete` is true. Assumption: #22 routes through those two predicates rather than reading `phase` and `repechage` itself. If it ever needs to open the phase without a pot — a host stepping into it to explain the rules, say — that is a scene, not a phase. |
| 55 | Issue #20 asks for a test where "5 winners, 5 losers, first three decline → fourth accepts, target reached" | **The sequence is tested; the conclusion cannot hold and is not asserted** (issue #20, `repechage.test.ts`). With `\|W\| = 5` the target is `2^ceil(log2(5)) = 8`, so `need` is 3 and one acceptance leaves two places open — after the fourth candidate accepts the field is 6 of 8 with one loser left in the pot. The test plays exactly the sequence the issue names and asserts what §4 actually produces, and a second test carries the intent — a field of 7 where the first three decline and the fourth accepts *does* reach its target of 8. Reasoning: the alternative is a test written to a number the rules do not produce, which would have to be made to pass by changing the rules. Assumption: the issue text is shorthand for "the draw loop survives a run of declines", which is what both tests check. |
| 56 | *Freilose vergeben* fills the field — but who actually receives the `Freilose`? | **Nobody, until the next round is drawn** (issue #20, docs/TOURNAMENT-RULES.md §4 fallback 1). The repechage records the decision and the tournament carries a *count*, `repechageState().byes`, equal to `target - \|W\|`. Reasoning: a `Freilos` is a property of a pairing, not of a group — §3 grants one to whoever the shuffle leaves over — so handing one out here would mean naming a recipient before the draw that decides it, which is the draw taking a decision away from the shuffle. It also has to be the full count and not the single bye an odd field earns: 13 groups short of 16 owe three `Freilose`, and a round drawn with one would produce 7 winners where the bracket needs 8. **Carried, not met, in #20:** the engine publishes the number and the invariant is stated over `\|W\| + Freilose`; the draw that consumes it is issue #22, and until it lands a tournament that took this fallback is short of its target at the next draw. Assumption: #22 reads `byes` off `repechageState` rather than re-deriving it from the phase. |
| 59 | The `REPECHAGE` scene descriptor was declared with a `roundId`, like `DRAW` and `ROUND_BOARD`. What round is that? | **None — the descriptor is now `{ id: 'REPECHAGE' }`** (issue #21, `@/domain/beamerScene`). The repechage is not a round: it produces no pairings, appends nothing to `rounds`, and everything it shows lives in `tournament.repechage`, of which a tournament has exactly one. The qualifying round it draws its losers from would have been the only candidate, and naming it would have been a promise the beamer could not keep — by then that round is `CLOSED`, so `currentRound` is null, the snapshot carries no round at all, and the staleness guard the other two scenes use (`tournament.round?.id === scene.roundId`) could never match. A scene that guarded on it would have rendered empty for the whole phase. Reasoning: the id existed because the union was written before the phase was, and carrying it would have meant either a guard that always fails or a field nothing reads. Nothing migrates: the scene lives in the store and on the window channel, never in the `.wattmatt` file. Assumption: a tournament never has two repechages, which docs/TOURNAMENT-RULES.md §1 fixes by putting the phase after `QUALIFYING` and nowhere else. |
| 60 | How does the beamer know whether it *watched* a repechage answer land, or arrived to find it already given? | **From `snapshot.delivery`, threaded to the scene, and not from `settled`** (issue #21, `useRepechageBeat`). The scene's beats are the host's clicks rather than a timeline, so each one arrives as an ordinary snapshot on a scene that is already up. `animate` — and therefore `settled` — is `delivery === 'live' && !sameScene`, which is false for *every* commit that leaves the same scene staged, so reading it would mean the `Hoffnungsrunde` never animated at all after its first frame. The hook instead names the beat — the group of the last draw plus what became of them — and plays anything that is not the beat this window is at rest on. `catchUp`, which is both a reopened beamer and an undo (issue #11), re-sets that resting state instead of playing into it, so correcting a misclick does not make the room watch somebody be turned down twice. Reasoning: the alternative is a timeline like `useDrawSequence`'s, but there is nothing to schedule — the pace is the host's. Assumption: issues #25 and #27 face the same question for the bracket and the ceremony and can take `delivery` the same way; if a third scene needs it, the threading is worth hoisting into `BeamerScenePlaceholder` as a context rather than a prop. |
| 61 | The round actions reserve `urgent` for a draw and a close. Why is *every* repechage action urgent? | **Because this is the phase the file cannot reconstruct** (issue #21, `@/store/actions/repechage`). The pot came out of one shuffle at one position of the seeded stream and every draw since has moved the cursor past it, so a laptop that crashed and lost a candidate cannot re-derive the pot it was showing — it can only shuffle again and offer the room a different name than the one that was just read out. The same is true of an answer: the field size and a group's status both move, and half a second of debounce is the difference between a recoverable event and a phase that has to be redone in front of the audience. Reasoning: the 500 ms debounce exists so a burst of clicks is one write, and this phase has no bursts — it is one deliberate decision every few seconds, with the room watching each one. Assumption: writing on each of perhaps twenty decisions is well inside what the autosave can absorb (issue #10). |
| 62 | docs/TOURNAMENT-RULES.md §9 case 5 says two groups play "one match, then straight to naming and a 2-slot final". Is that match the qualifying round? | **No — the qualifying round is not drawn at all, and the one match is the `Finale`** (issue #22, `@/domain/progression`). With exactly two participants `phaseStep` offers `QUALIFYING → NAMING` with a field of 2, and `drawBlockers` refuses the draw with `FINAL_PHASE_REACHED`. Reasoning: playing a qualifying round would leave a single group standing, and §5 would then start the final phase at a size of one — a bracket the app cannot draw and that §7 and §9 case 10 do not list. Reading the case the other way gives exactly what both documents ask for: one match, played as the `Finale`, no `Spiel um Platz 3` (§9 case 10). The rule is narrow on purpose — it fires only at two, so a field of 16 still plays its qualifying round and enters the final phase at 8, which is what §3 requires. Assumption: no host wants a two-participant event to have a "qualifying round" in its history; if one does, adding a third participant is the reversible answer and the app already allows it mid-tournament (§2). |
| 63 | `settings.namingAt` defaults to 16 and §5 ends the elimination rounds at 16. Does the naming threshold decide when `NAMING` is entered? | **No — the phase boundary is §5's 16 and nothing else** (issue #22, `FINAL_PHASE_SIZE` in `@/domain/draw`). `nextPhase` reaches `NAMING` when the field is at or below 16; `namingAt` decides when *names are required*, which is issue #23's question and docs/OPEN-QUESTIONS.md #8's answer. Reasoning: the two look like the same number because they share a default, but they answer different things. §7 names bracket rounds by field size — 16, 8, 4, 2 — so a final phase entered at 32 because a host wanted names early would have a round with no name; and a host who wants names from the start is asking to be *allowed* to type them, not to end the elimination rounds sooner. Assumption: issue #23 shows its naming panel from the moment the field reaches `namingAt`, whichever phase that falls in, and the bracket still refuses to be drawn until everybody has a name (§6). |
| 64 | The `ROUND_BOARD` scene names a round, but the snapshot only ever carried the open one. How is a past round put on the projector? | **The snapshot carries the closed rounds too, in `history`** (issue #22, `@/domain/snapshot`). `toTournamentSnapshot` sends every round whose state is `CLOSED`, with its matches, and `BeamerScenePlaceholder` hands `RoundBoardScene` whichever of them the descriptor names. Reasoning: the host is asked *wen hat sie in der zweiten Runde geschlagen?* at every tournament, and before this the projector answered with an empty board — the guard `tournament.round?.id === scene.roundId` could not match a round that was over. The alternative, deriving the snapshot from the staged scene, would make the beamer's copy depend on which picture is up and break the one rule the projection has: it is a function of the tournament, computed once, centrally (docs/ARCHITECTURE.md §3). No match travels twice — the open round is `round` plus `matches`, the history is what is over. Assumption: the added bytes stay well inside the one-frame budget, which `snapshotPerformance.test.ts` now measures with a full round of history in the payload. |
| 65 | docs/TOURNAMENT-RULES.md §6 says the bracket cannot be drawn until every remaining group has a name. Does `phaseStep` now offer `NAMING → BRACKET`? | **No — the gate is exported, the step is not** (issue #23, `isNamingComplete` in `@/domain/naming`). `phaseStep` still reports no step from `NAMING`, exactly as it did before: taking it would produce a `BRACKET` phase with `bracket: null`, which is a phase with nothing in it. What this issue adds is the condition the draw has to satisfy — every `ACTIVE` group has a name — plus the panel line that tells the host how many are still missing while they can still do something about it. Issue #24 draws the bracket and is the one caller. Reasoning: the alternative is a button that changes the phase and leaves the final phase empty until #24 lands, which is the opposite of "most reversible, least surprising". Assumption: #24 reads `isNamingComplete` rather than re-deriving it, so the panel's count and the draw's refusal cannot disagree in front of the room.
| 66 | Names reach the beamer at 64 px. What happens to one that is too long for the card? | **Step the type down to the 32 px floor, then let the ellipsis take over** (issue #23, `@/ui/nameFit`). The scenes' density ladders decide the base step from how crowded the board is; `fitNameType` steps *that* down for a name the line cannot hold, never below `text-beamer-body`; and `truncate` cuts whatever is still too long. `MAX_GROUP_NAME_LENGTH` is then chosen to be exactly what the floor holds, so a name a host can actually type never reaches the ellipsis at all. Reasoning: shrinking without a floor eventually produces a name that is on the wall and unreadable, which is worse than a visible ellipsis; truncating without shrinking loses the end of a name while two type steps of room are still going spare. `useFitToStage` scales the whole board on top of both and is a different problem — it fits the scene to the stage, not one name to one line. Assumption: the per-step character budgets are proportional to the type size and are approximate by nature; if a real tournament produces a name that still overflows, the budgets move, not the strategy.
| 67 | The example in issue #23's acceptance criteria — *Die schnellen Schnitzeljäger aus Salzburg* — is 41 characters, one over the 40 the same issue specifies. Which wins? | **The 40-character limit** (issue #23, docs/TOURNAMENT-RULES.md §6). The host field caps at 40 with `maxLength`, so that exact name is entered one character short. The beamer is tested with the full 41 all the same, because it renders whatever the file carries and a file repaired by hand can hold more (docs/FILE-FORMAT.md §Encoding) — that is the case the ellipsis exists for. Reasoning: §6 is normative and the example is illustrative, and raising the limit to fit one example would move the number the beamer's floor budget is derived from (#66). Assumption: 40 is generous enough in practice; if it is not, the change is one constant and the budgets follow it, but it goes through an issue because the rules document names the number.
| 68 | `roundKind` has a `BRACKET` value and `bracketNode` has a `tableId`. Does the final phase append a round to `rounds`, or is a node the match? | **The node is the match, and nothing is appended to `rounds`** (issue #24, `@/domain/bracket`). A node already carries everything a `Match` does — the two participants, the winner, the table it is played on — so a `BRACKET` round beside it would be a second record of the same result, and the two would eventually disagree about who won in front of the room. `table.currentMatchId` therefore names a node (`bn_3`) while a bracket match is on that table; the two id prefixes cannot collide, so one id still names one thing, and `bracketNodeMatch` (`@/domain/lookup`) is the `Match` view everything that reasons about matches keeps reading — the occupancy board, `matchesOnTables`, the snapshot the beamer's `TABLE_OVERVIEW` draws. `occupyTable` stays the one place a table is written and now sets the node's `tableId` alongside, so a table taken out of service mid-`Halbfinale` moves or requeues the node exactly as it does a match (issue #13). Reasoning: the alternative reuses the round panel and the round history for free, but pays for it with two winners fields to keep in step during the half of the evening that is hardest to undo. The `BRACKET` value stays in `roundKindSchema` — a file written by hand, and the archived fixtures in `tests/fixtures/`, contain one, and a value nothing produces costs nothing while removing it would cost a migration. Assumption: issues #25 and #26 draw the tree from `bracket.nodes` and do not expect a `Round` to hang it on. |
| 69 | The loser of a `Halbfinale` has lost — does `setBracketWinner` mark them `ELIMINATED`? | **No: they stay `ACTIVE` until the `Spiel um Platz 3` is played** (issue #24, `@/domain/bracket`). The rule is "a group is `ELIMINATED` when it loses and has nowhere left to play", which is what every earlier phase already does and what makes the semi-final the one exception: its loser is routed straight into the third-place match in the same object that decides the semi. Reasoning: a participant marked out of the tournament who then appears on the projector in a match is a contradiction the whole room can see, and `activeGroups` is what the panels count. It follows that the evening ends with two participants still `ACTIVE` — the champion and whoever won bronze — because neither of them lost their last match. The podium is not read off `status` in any case: `finalStandings` reads the three places off the three nodes that decided them, because second place is *lost* rather than won (§7, §8). Assumption: issue #27 takes the podium from `finalStandings` rather than from who is left `ACTIVE`. |
| 70 | docs/MOTION.md §4.4 and issue #25 both ask for the advancement to use a Motion `layoutId`. Motion is not a dependency — is it added? | **No: the chip is moved with a measured FLIP** (issue #25, `useBracketAdvance`). Every beamer beat so far is CSS driven by a token, and `framer-motion` has never been installed even though CLAUDE.md §3 lists it; adding it for one transition would leave two motion systems where the second one owns exactly one effect. The FLIP does what `layoutId` does internally — read the chip's old and new rectangles, put it back with a transform, let a transition carry it home — and the acceptance criterion is about what the audience sees ("never fade out/in", "chips jump instead of flying" under reduced motion), which it meets exactly. What is given up is the spring's `bounce: 0.15`: the ride is `--ease-dramatic` over `--dur-reveal`, so performance mode halves it for free (issue #15) and reduced motion removes it in CSS. Reasoning: a dependency is the least reversible thing in this list — a hook is deleted in one commit. If a later scene needs shared-element transitions too, that is the moment to install the library and move both. Assumption: issue #29's performance pass measures the FLIP at 16 chips like any other transform animation; it composites, so it should be free.
| 71 | The other snapshot fields are *derived* views (`repechage`, `round`). Why does the beamer get the raw `Bracket`? | **Because the derivation is pure and both sides run it** (issue #25, `bracketColumns` in `@/domain/bracket`). The repechage snapshot is derived because its picture — who is through, how many places are left — is arithmetic over records the beamer has no other way to reach. The bracket is not: the tree *is* the picture, and everything the scene adds on top of it (the columns, which round is live, where a chip travelled from) is a pure function of the same object, which the host panel (#26) has to call as well. Sending the tree and calling that function in both windows is one definition; sending a derived tree would be a second one to keep in step. Reasoning: it is also the smaller payload — at most 16 nodes against a column list that repeats every one of them. Assumption: the 16 ms one-frame budget still holds, and `snapshotPerformance.test.ts` now measures a payload with a full 16-slot bracket in it.
| 72 | Issue #24 refused a bracket correction once the result had been played on, and pointed the host at undo. Issue #26 asks for the correction *with* a warning. Which is it? | **The correction goes through, and it discards what was built on it** (issue #26, `setBracketWinner` and `bracketCorrection` in `@/domain/bracket`). Marking the other participant clears every result above the corrected node — the node above, and from a `Halbfinale` the `Spiel um Platz 3` — puts those matches back in the queue with the corrected pairings, and frees the tables they were on. The walk only ever climbs the links out of the node it starts at, so the other half of the tree is untouched, which is the issue's first acceptance criterion stated as an algorithm. Reasoning: undo *works*, but a quarter-final noticed an hour late would cost the host one press per decision since, backwards, in front of the room — and every one of those presses takes something else back as well. What #24 was actually protecting against was a silent rewrite, and the answer to that is not a refusal but a question: `bracketCorrection` runs the same walk and hands back the list of results that would go, and the panel names them in a dialog before anything happens. It answers null when nothing would be discarded, which is the ordinary correction and stays a single click — a dialog in front of every result is a dialog that gets dismissed unread. Undo still takes the whole thing back in one press (golden rule 6). Assumption: a host who wants the old refusal back has undo; nobody has asked for a lock.
| 73 | `phaseStep` still reports no step from `BRACKET`. What moves the tournament into the `Siegerehrung`? | **`finishBracket`, from the bracket panel** (issue #26, `@/domain/bracket`). *Finale abschließen* sits under the final the host has just decided, gated on `isBracketComplete`, and moves the phase and nothing else. Reasoning: the same argument that kept `NAMING → BRACKET` out of `phaseStep` (docs/OPEN-QUESTIONS.md #65). The phase panel offers one generic step with one set of reasons for being greyed out; a second button for the same transition would be a second set, and the host would have two places to look for the one that is refusing. It also keeps §8 intact — the transition is not the reveal, so nothing is staged on the projector and the finished tree stays up until somebody puts the podium on it (issue #27). Assumption: #27 stages the `CEREMONY` scene itself, from the ceremony panel, and does not expect this action to have done it.
| 74 | Issue #26's round-focus control "zooms the beamer to a round". Does the projector then show that round alone? | **It shows that round and everything after it** (issue #26, the `focus` of the `BRACKET` scene descriptor). Zooming to the `Halbfinale` of a field of 16 draws the semi-finals, the `Spiel um Platz 3` and the `Finale` — four matches on a screen that was holding sixteen, which is what "so the last matches fill the screen" asks for. Reasoning: a single column stripped of its links would be a list of matches rather than a tree, and the audience would lose the one thing the picture is for — seeing where the winner in front of them is going. The third-place match is kept whenever the final is drawn, because it sits *before* the final in the tree's own order and §7 plays the two together, so a plain slice would cut off half of what the zoom is about. The host's control offers every round but the `Spiel um Platz 3`: "from the third-place match onwards" is not a thing the tree can mean. Assumption: `focus` stays out of the file — it is a property of what the projector is showing, like every other part of a scene descriptor, and a reopened beamer is handed it in the next snapshot.
| 75 | Does a freeze travel with an undo, the way a staged scene does (#33)? | **No** (issue #28, `setFrozen`). A freeze is a hold the host is applying with their hand still on the key, not a decision about the evening — so it commits without an undo label and `frozen` stays out of `UndoSnapshot`. The alternative loses the room: an undo aimed at a misclicked result three panels away would also whip the cover off a screen the host is deliberately working behind, which is the "auto-follow must never yank the screen away" failure by another route. It is its own undo — the host presses the same control again — which is the property golden rule 6 is actually about. Assumption: if a host ever reports losing track of a freeze because it did *not* come back with an undo, the answer is a visible reminder rather than putting it on the stack; the panel already carries one, and it shouts. |
| 76 | `autoFollow` says "the scene follows the tournament phase". Follows it *when*? | **Only at a phase boundary, and when the host turns auto-follow back on** (issue #28, `sceneForPhase` in `@/domain/sceneCatalog`, `advancePhase`). Not on every commit: a host who put the `Turnierbaum` on the wall to talk over it would lose it the moment a result landed, which the issue forbids outright ("auto-follow must never yank the screen away mid-explanation"). A phase step is a button the host pressed themselves, so it is the one moment where a picture change cannot surprise them. Inside a phase the picture belongs to whoever staged it — the draw stages its own sequence, and a manual choice turns `autoFollow` off and outranks everything until released. `SETUP` maps to `IDLE` rather than to the field of participants, because a projector that switched itself on because a tournament was created is a surprise, not a service. |
| 77 | The host's `Space` shortcut is registered on the window. `Space` also activates whichever control has focus — which wins? | **The skip wins** (issue #28, `useBeamerShortcuts`). Nothing becomes unreachable: `Enter` activates a focused button, and every shortcut is off entirely while the host is in a text field. The alternative — leaving `Space` to the focused control — is worse in both directions. It would mean the shortcut the issue asks for silently does nothing for most of an event, because the host's focus is almost always on the button they last clicked; and that button is usually *Sieger festlegen*, so a stray `Space` would mark a result rather than skip an animation. Assumption: if a host reports missing `Space`-to-activate, the answer is to blur the control after a click rather than to give the key back. |
